# 環境開発構築手順書（旅行先提案Webシステム / GeoGuessr風）

本書は、本システムを **Ubuntu サーバー上に構築し、起動できる状態にする** ための手順をまとめたものである。

---

## 1. 動作環境

### 1.1 サーバー要件

| 項目 | 内容 |
|------|------|
| OS | Ubuntu 22.04 LTS / 24.04 LTS |
| Node.js | v18 以上 |
| データベース | MariaDB 10.6 以上 |
| Webサーバー | 任意（Nginx / Caddy 等） |
| ストレージ | 10GB 以上の空き容量 |

---

## 2. 事前準備

### 2.1 Node.js のインストール

```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
node -v
npm -v
```

---

## 3. MariaDB の構築

### 3.1 MariaDB のインストール

```bash
sudo apt update
sudo apt install -y mariadb-server mariadb-client
sudo systemctl enable --now mariadb
sudo systemctl status mariadb --no-pager
```

### 3.2 初期設定（推奨）

```bash
sudo mysql_secure_installation
```

### 3.3 MariaDB にログイン

```bash
sudo mariadb
```

---

## 4. データベース作成・ユーザー作成

> ※ DB名・ユーザー名・パスワードは環境に合わせて変更すること。  
> ※ 本システムの `.env` 例では `DB_NAME=website` を使用している。

### 4.1 データベース作成

```sql
CREATE DATABASE IF NOT EXISTS website
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_general_ci;

USE website;
```

### 4.2 アプリ用ユーザー作成（例）

```sql
CREATE USER IF NOT EXISTS 'geoapp'@'localhost' IDENTIFIED BY 'Password';
GRANT ALL PRIVILEGES ON website.* TO 'geoapp'@'localhost';
FLUSH PRIVILEGES;
```

---

## 5. テーブル作成（website）

本項は `SHOW TABLES` と `DESC` の結果に基づき、同一構造になるように `CREATE TABLE` をまとめたものである。  
※ 外部キー（FOREIGN KEY）が設定されているかどうかは `DESC` だけでは確定できないため、本手順では **外部キーは作成していない**（必要に応じて追加する）。

### 5.1 users テーブル

```sql
CREATE TABLE IF NOT EXISTS USERS (
  id VARCHAR(36) NOT NULL,
  mail_address VARCHAR(255) NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  continuous_login_days INT(11) NOT NULL DEFAULT 0,
  avatar_url VARCHAR(2048) NULL,
  location_lat DOUBLE NULL,
  location_lng DOUBLE NULL,
  uuid VARCHAR(36) NOT NULL,
  email_verified TINYINT(1) NOT NULL DEFAULT 0,
  email_verify_token VARCHAR(64) NULL,
  email_verify_expires_at DATETIME NULL,
  PRIMARY KEY (uuid),
  UNIQUE KEY uq_users_id (id),
  UNIQUE KEY uq_users_mail_address (mail_address)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 5.2 spots テーブル

```sql
CREATE TABLE IF NOT EXISTS spots (
  spot_id         INT(11)       NOT NULL AUTO_INCREMENT,
  title           VARCHAR(100)  NOT NULL,
  genre           VARCHAR(50)   NULL,
  description     TEXT          NOT NULL,
  lat             DOUBLE        NOT NULL,
  lng             DOUBLE        NOT NULL,
  image_path      VARCHAR(255)  NOT NULL,
  street_view_url TEXT          NULL,
  created_at      DATETIME      NULL DEFAULT CURRENT_TIMESTAMP,

  PRIMARY KEY (spot_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 5.3 user_answers テーブル

```sql
CREATE TABLE IF NOT EXISTS user_answers (
  id          INT(11)        NOT NULL AUTO_INCREMENT,
  spot_id     INT(11)        NOT NULL,
  answer_lat  DECIMAL(10,8)  NOT NULL,
  answer_lng  DECIMAL(11,8)  NOT NULL,
  distance_km DECIMAL(6,2)   NOT NULL,
  score       INT(11)        NOT NULL,
  answered_at DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,
  user_uuid   VARCHAR(36)    NULL,

  PRIMARY KEY (id),
  KEY idx_user_answers_spot_id (spot_id),
  KEY idx_user_answers_user_uuid (user_uuid)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 5.4 email_verification_codes テーブル

```sql
CREATE TABLE IF NOT EXISTS email_verification_codes (
  token      CHAR(64)                               NOT NULL,
  email      VARCHAR(255)                           NOT NULL,
  user_id    VARCHAR(50)                            NULL,
  code       CHAR(6)                                NOT NULL,
  purpose    ENUM('register','reset_password')      NOT NULL,
  expires_at DATETIME                               NOT NULL,
  used       TINYINT(1)                             NOT NULL DEFAULT 0,
  created_at DATETIME                               NOT NULL DEFAULT CURRENT_TIMESTAMP,

  PRIMARY KEY (token)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 5.5 account_change_tokens テーブル

```sql
CREATE TABLE IF NOT EXISTS account_change_tokens (
  id         INT(10) UNSIGNED                       NOT NULL AUTO_INCREMENT,
  user_uuid  CHAR(36)                                NOT NULL,
  token      CHAR(64)                                NOT NULL,
  kind       ENUM('username','email','password','delete') NOT NULL,
  expires_at DATETIME                                NOT NULL,
  consumed   TINYINT(1)                              NOT NULL DEFAULT 0,
  created_at TIMESTAMP                               NOT NULL DEFAULT CURRENT_TIMESTAMP,

  PRIMARY KEY (id),
  UNIQUE KEY uq_account_change_tokens_token (token),
  KEY idx_account_change_tokens_user_uuid (user_uuid)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 5.6 作成確認

```sql
SHOW TABLES;

DESC users;
DESC spots;
DESC user_answers;
DESC email_verification_codes;
DESC account_change_tokens;
```

---

## 6. アプリケーションの設置

### 6.1 ソースコードの配置

```bash
cd /var/www
git clone https://github.com/20234033/-web-.git
cd geoguessr-app
```

### 6.2 依存関係のインストール

```bash
npm ci
```

---

## 7. 環境変数の設定

`.env` ファイルを作成し、必要項目を設定する。

```bash
cp .env.example .env
nano .env
```

設定例：

```env
GOOGLE_API_KEY=
RAKUTEN_APP_ID=

DB_HOST=localhost
DB_USER=geoapp
DB_PASSWORD=Password
DB_NAME=website

SECRET_KEY=

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
S3_BUCKET_NAME=

JWT_SECRET=

GEMINI_API_KEY=

MAILERSEND_API_KEY=
VERIFY_FROM_EMAIL=
VERIFY_FROM_NAME=旅行先提案Webシステム
```

---

## 8. データベース初期化（スクリプトがある場合）

プロジェクト側にマイグレーション / 初期化スクリプトがある場合は実行する。

```bash
npm run migrate
# または
node scripts/init-db.js
```

足りないもの
npm install bcrypt
npm install mariadb
npm install cors


---

## 9. 起動方法

### 9.1 開発モード

```bash
npm run dev
```

### 9.2 本番モード

```bash
npm run build
npm start
```

### 9.3 PM2 を使用する場合

```bash
npm install -g pm2
pm2 start npm --name geo-app -- start
pm2 save
pm2 status
```

---

## 10. 動作確認

ブラウザで以下にアクセスし、トップ画面が表示されることを確認する。

```text
http://<サーバーIP>:3000
```

---

## 11. トラブルシューティング

| 症状 | 原因 | 対処方法 |
|------|------|----------|
| 画面が表示されない | アプリが起動していない | `npm start` または `pm2 status` で起動状態を確認する |
| 画面が表示されない | ポートが閉じている | `ufw status` で 3000 番ポートが許可されているか確認する |
| DB 接続エラー | `.env` の DB 設定が間違っている | `DB_HOST` / `DB_USER` / `DB_PASSWORD` / `DB_NAME` を確認する |
| DB 接続エラー | MariaDB が起動していない | `sudo systemctl status mariadb` で状態を確認する |
| ログインできない | JWT_SECRET が変更された | `.env` の `JWT_SECRET` を固定し、再起動する |
| 500 エラーが出る | サーバー側で例外が発生している | `pm2 logs` またはコンソールログを確認する |
| 画像が表示されない | 参照パスが間違っている | 画像の配置場所と参照パスを確認する |
| 保存できない | DB の権限不足 | `GRANT` が正しいか確認する |
| 変更が反映されない | PM2 を再起動していない | `pm2 restart geo-app` を実行する |

---

## 12. バックアップ（任意）

```bash
mysqldump -u geoapp -p website > website_backup.sql
```